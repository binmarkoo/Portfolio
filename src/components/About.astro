---
const personalInfo = [
  { label: 'Name', value: 'Marko Mikanovic', icon: '👤' },
  { label: 'Alter', value: '20 Jahre', icon: '🎂' },
  { label: 'Beruf', value: 'Schüler - Softwareentwicklung', icon: '💻' },
  { label: 'Standort', value: 'Bischofshofen, Österreich', icon: '📍' }
];

const socialLinks = [
  { 
    name: 'Gmail', 
    url: 'mailto:marko.mikanovic2309@gmail.com',
    image: '/images/contact/gmail.png',
    color: 'from-blue-400 to-cyan-400'
  },
  { 
    name: 'LinkedIn', 
    url: 'https://www.linkedin.com/in/marko-mikanovic-4509aa340/',
    image: '/images/contact/linkedin.png',
    color: 'from-blue-600 to-blue-700'
  },
  { 
    name: 'GitHub', 
    url: 'https://github.com/binmarkoo',
    image: '/images/contact/github.png',
    color: 'from-gray-700 to-gray-900'
  }
];
---

<section class="min-h-screen flex items-center py-16 md:py-20 bg-gray-950">
  <div class="container-custom">
    <!-- <h2 class="text-3xl sm:text-4xl md:text-5xl font-bold text-center mb-12 md:mb-16 px-4">
      Über <span class="gradient-text">Mich</span>
    </h2> -->
    
    <div class="max-w-6xl mx-auto px-4">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 items-stretch">
        
        <!-- Linke Seite: Bild & Social Links -->
        <div class="flex flex-col gap-6">
          <!-- Profilbild - Nimmt verfügbaren Platz ein -->
          <div class="flex-1 flex items-center justify-center lg:justify-start">
            <div class="relative group w-full max-w-md">
              <div class="absolute -inset-1 bg-gradient-to-r from-blue-500 via-purple-500 to-pink-500 rounded-2xl blur opacity-25 group-hover:opacity-50 transition duration-500"></div>
              <div class="relative">
                <img 
                  src="/images/Marko.jpg" 
                  alt="Marko Profilbild"
                  class="w-full aspect-[3/4] object-cover rounded-2xl border-4 border-white/10"
                />
              </div>
            </div>
          </div>
          
          <!-- Social Links mit Bildern -->
          <div class="flex flex-wrap justify-center lg:justify-start gap-3 md:gap-4">
            {socialLinks.map(link => (
              <a 
                href={link.url}
                target={link.url.startsWith('http') ? '_blank' : undefined}
                rel={link.url.startsWith('http') ? 'noopener noreferrer' : undefined}
                class={`flex items-center gap-2 md:gap-3 px-5 md:px-6 py-2.5 md:py-3 bg-gradient-to-r ${link.color} rounded-full text-white text-sm md:text-base font-medium hover:scale-105 hover:shadow-lg transition-all duration-300`}
              >
                <img 
                  src={link.image} 
                  alt={link.name}
                  class="w-5 h-5 md:w-6 md:h-6 object-contain"
                />
                <span>{link.name}</span>
              </a>
            ))}
          </div>
        </div>
        
        <!-- Rechte Seite: Informationen - Gleiche Höhe wie linke Seite -->
        <div class="flex flex-col gap-6">
          
          <!-- Persönliche Infos -->
          <div class="card-glass p-6 md:p-8 space-y-5 md:space-y-6">
            {personalInfo.map(info => (
              <div class="flex items-center gap-3 md:gap-4 group">
                <div class="text-2xl md:text-3xl group-hover:scale-110 transition-transform duration-300 flex-shrink-0">
                  {info.icon}
                </div>
                <div class="flex-1 min-w-0">
                  <p class="text-xs md:text-sm text-gray-400 uppercase tracking-wider mb-1">{info.label}</p>
                  <p class="text-base md:text-lg text-white font-medium break-words">{info.value}</p>
                </div>
              </div>
            ))}
          </div>
          
          <!-- Über mich Text - Ohne Titel -->
          <div class="card-glass p-6 md:p-8 flex-1">
            <h3 class="text-xl md:text-2xl font-bold text-white mb-3 md:mb-4 flex items-center gap-2">
              <span class="text-blue-500">●</span> Über mich
            </h3>
            <p class="text-base md:text-lg text-gray-300 leading-relaxed">
              Ich liebe es, Zeit in durchdachte und ästhetische Designs zu investieren und kreative Ideen visuell zum Leben zu erwecken. Besonders im Frontend-Development finde ich meine Leidenschaft, weil ich dort Gestaltung und Technik perfekt verbinden kann.
            </p>
          </div>
          
        </div>
      </div>
    </div>
  </div>
</section>